#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "bib_vetores.h"

void gerar_valor_vet(int tamanho, int vetor[], int valor_limite) {

    srand(time(NULL));


 for(int i = 0; i < tamanho; tamanho ++) {

        int aleatório = 1 + rand() % valor_limite;

        vetor[tamanho] = aleatório;
        
        

    }

}

void multiplicar_vetores(int tamanho, int a[], int b[]) {

    for (tamanho = 0; tamanho )


} 